build:
    box:
        id: busybox
        cmd: /bin/sh
    steps:
        - script:
            code: |
                echo "$SECRET"
                if [ "$SECRET" != "this is really secret" ]; then
                    exit 1
                fi
