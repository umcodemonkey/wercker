build:
    box:
        id: busybox
        cmd: /bin/sh
    steps:
        - script:
            code: |
                echo -n "$SECRET"
                if [ "$SECRET" != "HIDDEN" ]; then
                    exit 1
                fi
