build:
    box:
        id: busybox
        cmd: /bin/sh
    steps:
        - script:
            code: |
                echo "$SECRET"
                if [ ! -z "$SECRET" ]; then
                    exit 1
                fi
